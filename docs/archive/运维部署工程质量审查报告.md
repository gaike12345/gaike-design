# AI 漫剧圈（Mank TV）运维·部署·工程质量 全面审查报告

> 审查日期：2026-09-03
> 审查范围：Docker / docker-compose / 部署脚本 / 环境管理 / 数据库 / 监控日志 / 安全基线 / 依赖安全
> 审查方式：只读静态分析

---

## 一、审查总览

| 维度 | 评分（10分制） | 高危问题数 | 中危问题数 | 低危问题数 |
|------|:---:|:---:|:---:|:---:|
| Docker 配置 | 7.0 | 1 | 2 | 2 |
| docker-compose | 6.5 | 2 | 3 | 2 |
| 部署流程 | 6.0 | 1 | 3 | 2 |
| 环境管理 | 4.5 | **3** | 2 | 1 |
| 数据库设计 | 7.5 | 1 | 3 | 3 |
| 监控与日志 | 7.0 | 0 | 2 | 3 |
| 安全基线 | 7.5 | 2 | 3 | 2 |
| 依赖安全 | 6.5 | 1 | 2 | 2 |

**总体评价：** 项目具备较为完善的安全意识和基础工程能力，但在**敏感信息管理**、**资源隔离**、**回滚机制**三个方面存在明显短板，建议优先修复高危问题后再投入生产环境。

---

## 二、高危问题（必须立即修复）

### 🔴 H-1：真实 API Key 明文提交到代码仓库

**严重级别：高**
**位置：** `server/.env` 第 32 行

```
ZHIPU_API_KEY=761491ceae334aeca18ca84215284796.0sPJ9Jle1oRkpIc3
```

**问题描述：**
- 开发环境的 `.env` 文件中包含真实的智谱 AI API Key，且该文件**未被 .gitignore 正确排除**
- 虽然根目录 `.gitignore` 第 27 行有 `.env` 规则，但 `server/.env` 仍然存在于工作目录中，说明可能曾被提交过
- 该 API Key 一旦泄露，攻击者可直接消耗调用额度，产生经济损失

**风险：** 经济损失 + 数据泄露（调用记录可能包含用户输入内容）

**修复建议：**
1. 立即在智谱控制台吊销该 API Key 并生成新 Key
2. 确认 `.gitignore` 中 `server/.env` 被正确忽略
3. 检查 Git 历史中是否包含该密钥，如有则需清理历史（`git filter-repo` 或 BFG Repo-Cleaner）

---

### 🔴 H-2：生产环境 .env 模板暴露在代码仓库中

**严重级别：高**
**位置：** `.env.production`（根目录）

**问题描述：**
- 根目录 `.env.production` 文件包含完整的生产环境变量结构
- 虽然值为占位符，但该文件暴露了系统的完整配置结构（数据库连接、AI 供应商、支付配置等）
- `.gitignore` 第 32 行的 `.env.*` 规则与第 33 行的 `!.env.example` 存在矛盾——`.env.production` 应该被忽略但实际未被忽略
- 攻击者可通过该文件了解系统架构，缩小攻击面

**风险：** 信息泄露 + 攻击面缩小辅助

**修复建议：**
1. 将 `.env.production` 重命名为 `.env.production.example` 或移动到 `docs/` 目录作为示例
2. 确保 `.env.*` 规则正确生效，仅 `!.env.example` 例外

---

### 🔴 H-3：默认密码硬编码且强度极低

**严重级别：高**
**位置：** `deploy/server-deploy.sh` 第 330-331 行、`server/src/index.ts` 第 221-224 行

```bash
# server-deploy.sh
普通用户：demo@manktv.com / password123
管理员：admin@manktv.com / password123
```

**问题描述：**
- 部署脚本硬编码了演示账号密码 `password123`，强度极低
- 后端根路径在开发模式下直接展示演示账号
- 若生产环境未修改默认密码，攻击者可直接登录管理员账号
- 唯一超级管理员邮箱 `admin@manktv.com` 也是公开的固定值

**风险：** 账号接管 + 数据泄露 + 系统完全控制

**修复建议：**
1. 部署脚本首次部署时**强制生成随机密码**并输出给管理员
2. 种子数据中默认密码应通过环境变量传入，不使用固定值
3. 首次登录强制修改密码（FIM - First Login Must-change）
4. 生产环境根路径不展示任何账号信息

---

### 🔴 H-4：Redis 无密码认证且端口直接暴露

**严重级别：高**
**位置：** `docker-compose.yml` 第 106-123 行

```yaml
redis:
  image: redis:7-alpine
  command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
  ports:
    - "6379:6379"  # 直接暴露到宿主机
```

**问题描述：**
- Redis 未设置密码（`requirepass`）
- 端口 `6379` 直接映射到宿主机，公网可访问
- 若攻击者扫描到 Redis 端口，可直接读取/修改所有限流数据、任务队列数据
- 甚至可能通过 Redis 写入 webshell（如果 Redis 以 root 运行且有磁盘写入权限）

**风险：** 数据泄露 + 限流绕过 + 潜在 RCE

**修复建议：**
1. 配置 Redis 密码：`command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes ...`
2. 生产环境下移除 `ports` 映射（仅容器内部访问），或绑定到 `127.0.0.1:6379:6379`
3. 启用 `protected-mode yes`

---

### 🔴 H-5：CORS 子域名匹配逻辑存在绕过风险

**严重级别：高**
**位置：** `server/src/index.ts` 第 67-76 行

```typescript
if (IS_PROD) {
  try {
    const allowedUrl = new URL(allowed)
    const originUrl = new URL(origin)
    return (
      originUrl.hostname.endsWith(allowedUrl.hostname) &&  // 风险点
      originUrl.protocol === allowedUrl.protocol &&
      originUrl.port === allowedUrl.port
    )
  } catch {
    return false
  }
}
```

**问题描述：**
- 使用 `endsWith` 进行子域名匹配，可能导致前缀绕过
- 例如：允许 `example.com`，则 `evil-example.com` 也会匹配（因为 `evil-example.com` endsWith `example.com`）
- 生产环境子域名匹配是高风险操作，应使用更严格的匹配规则

**风险：** CORS 绕过 + 跨站数据窃取

**修复建议：**
```typescript
// 正确的子域名匹配：完全相等 或 点号前缀
return (
  originUrl.hostname === allowedUrl.hostname ||
  originUrl.hostname.endsWith('.' + allowedUrl.hostname)
)
```

---

### 🔴 H-6：文件上传仅校验扩展名，无 MIME/魔数检测

**严重级别：高**
**位置：** `server/src/middleware/upload.ts` 第 21-28 行

```typescript
fileFilter: (_req, file, cb) => {
  const allowed = /\.(jpg|jpeg|png|gif|webp|mp3|wav|m4a|mp4|mov|pdf|txt|epub)$/i
  if (allowed.test(path.extname(file.originalname))) {
    cb(null, true)
  } else {
    cb(new Error('不支持的文件类型'))
  }
},
```

**问题描述：**
- 仅通过文件扩展名判断文件类型，攻击者可将恶意脚本重命名为 `.jpg` 上传
- 虽然 Nginx 和 Express 静态服务不会执行 PHP/ASP，但如果存在其他解析漏洞则可能被利用
- 尤其 `pdf`、`txt`、`epub` 等格式可能包含恶意内容（如 PDF 中的 JavaScript、SVG XSS）
- 缺少文件内容验证（魔数检测 / MIME 嗅探）

**风险：** 恶意文件上传 + 潜在 XSS（SVG/HTML 伪装为图片）

**修复建议：**
1. 使用 `file-type` 或 `mmmagic` 库检测文件真实类型
2. 对图片文件进行二次处理（如使用 sharp 重新编码），破坏潜在的恶意代码
3. SVG 文件需经过 XML 净化（sanitize）后再提供访问

---

## 三、中危问题（建议尽快修复）

### 🟡 M-1：Docker 容器以 root 用户运行

**严重级别：中**
**位置：** `server/Dockerfile`、`web/Dockerfile`

**问题描述：**
- 两个 Dockerfile 均未创建非 root 用户，容器内进程默认以 root 运行
- 若应用存在 RCE 漏洞，攻击者直接获得容器内 root 权限
- 违反最小权限原则

**修复建议：**
```dockerfile
# server Dockerfile 添加
RUN addgroup -S app && adduser -S app -G app
USER app
```

---

### 🟡 M-2：docker-compose 无资源限制

**严重级别：中**
**位置：** `docker-compose.yml`（所有服务）

**问题描述：**
- 所有服务均未配置 `mem_limit`、`cpus`、`memswap_limit` 等资源限制
- 单个服务（如后端内存泄漏、AI 生成任务堆积）可能耗尽宿主机资源
- 缺少 `pids_limit` 防止 fork bomb

**修复建议：**
```yaml
backend:
  deploy:
    resources:
      limits:
        cpus: '2.0'
        memory: 1G
      reservations:
        cpus: '0.5'
        memory: 256M
```

---

### 🟡 M-3：缺少部署回滚机制

**严重级别：中**
**位置：** `deploy/update.sh`、`deploy/deploy.sh`

**问题描述：**
- `update.sh` 直接拉取最新代码并重新构建，没有版本回滚能力
- 如果新版本有 bug，无法快速回退到上一个稳定版本
- Docker 部署模式下没有镜像版本标签（使用 latest）
- PM2 部署模式下没有备份上一版 `dist/` 目录

**修复建议：**
1. Docker 模式：使用 Git commit hash 或版本号作为镜像标签，保留最近 N 个版本镜像
2. PM2 模式：部署前备份 `dist/` 目录，提供 `rollback.sh` 脚本
3. 使用蓝绿部署或金丝雀发布策略

---

### 🟡 M-4：无 CI/CD 配置

**严重级别：中**
**位置：** 项目根目录（无 `.github/workflows/`、`.gitlab-ci.yml` 等）

**问题描述：**
- 缺少自动化测试、构建、部署流程
- 代码质量完全依赖人工审查
- 没有 lint、typecheck、单元测试的自动化门禁
- 手动部署容易遗漏步骤（如数据库迁移）

**修复建议：**
1. 添加 GitHub Actions / GitLab CI 配置
2. 至少包含：`npm ci` → `typecheck` → `lint` → `build` → （可选）单元测试
3. 生产部署配置手动触发的 workflow_dispatch

---

### 🟡 M-5：备份仅本地存储，无异地备份

**严重级别：中**
**位置：** `deploy/backup.sh` 第 69-73 行（注释掉的 S3 同步）

**问题描述：**
- 备份脚本只备份到本地 `/var/backups/manktv`
- 如果服务器磁盘损坏或被入侵，备份同时丢失
- 备份保留 30 天，但没有验证备份可用性的机制
- SQLite 备份使用 `gzip -c` 直接复制数据库文件，在写入时备份可能导致数据不一致

**修复建议：**
1. 启用远程备份（S3/OSS/COS），使用 SSE 加密
2. SQLite 备份应使用 `.backup` 命令或 `VACUUM INTO` 保证一致性
3. 定期进行恢复演练（至少每月一次）

---

### 🟡 M-6：bcryptjs 替代 bcrypt，存在性能和安全差异

**严重级别：中**
**位置：** `server/package.json` 第 27 行

```json
"bcryptjs": "^2.4.3"
```

**问题描述：**
- `bcryptjs` 是纯 JavaScript 实现，比原生 `bcrypt` 慢 2-3 倍
- 纯 JS 实现的密码哈希更容易被侧信道攻击（理论风险）
- `bcryptjs` v2.4.3 最后更新于 2017 年，长期未维护
- 为了更好的安全性，建议使用原生 `bcrypt` 或 `argon2`

**修复建议：**
1. 替换为 `bcrypt`（原生绑定，更快更安全）
2. 或升级到 `argon2`（抗 GPU 攻击能力更强，OWASP 推荐）

---

### 🟡 M-7：全局 traceId 使用 globalThis，并发请求会互相覆盖

**严重级别：中**
**位置：** `server/src/lib/logger.ts` 第 145 行、`server/src/middleware/request-id.ts` 第 52 行

```typescript
// request-id.ts
;(globalThis as any).__traceId = requestId

// logger.ts
traceId: (globalThis as any).__traceId || undefined,
```

**问题描述：**
- `traceId` 存储在 `globalThis` 上，是全局共享的
- Node.js 单线程并发处理多个请求时，后一个请求会覆盖前一个请求的 traceId
- 导致日志中的 traceId 错乱，无法正确追踪请求链路
- 应使用 `AsyncLocalStorage` 或 `cls-hooked` 实现请求上下文隔离

**风险：** 日志链路追踪失效，排查问题困难

**修复建议：**
```typescript
import { AsyncLocalStorage } from 'async_hooks'
const asyncLocalStorage = new AsyncLocalStorage()
// 在中间件中使用 asyncLocalStorage.run(requestId, next)
// 在 logger 中使用 asyncLocalStorage.getStore() 获取 traceId
```

---

### 🟡 M-8：Prisma schema 默认使用 SQLite，生产切换需手动迁移

**严重级别：中**
**位置：** `server/prisma/schema.prisma` 第 7-10 行

```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
```

**问题描述：**
- Schema 中硬编码 `provider = "sqlite"`，切换到 PostgreSQL 需要修改 schema 文件
- 虽然提供了 `migrate-sqlite-to-pg.ts` 脚本，但迁移过程复杂
- SQLite 在并发写入、事务隔离级别等方面能力有限，不适合生产高并发场景
- 迁移文件是 SQLite 语法，PostgreSQL 部署时需要重新生成迁移

**修复建议：**
1. 将默认 provider 改为 PostgreSQL，SQLite 作为开发备选
2. 或使用 Prisma 的多 provider 支持（需注意兼容性）
3. 生产环境文档中明确标注 PostgreSQL 为推荐数据库

---

### 🟡 M-9：错误处理中间件使用 console.error 而非结构化日志

**严重级别：中**
**位置：** `server/src/middleware/error.ts` 第 10 行

```typescript
console.error('[Error]', err.message, '\n', err.stack)
```

**问题描述：**
- 全局错误处理使用 `console.error` 而非项目的 `logger` 工具
- 错误日志没有结构化（JSON 格式），不利于日志采集和告警
- 缺少 traceId 关联，无法将错误与请求链路对应

**修复建议：**
```typescript
import logger from '../lib/logger'
logger.error('未捕获异常', { 
  error: err.message, 
  stack: err.stack,
  // 从 req 中获取更多上下文
})
```

---

## 四、低危问题（建议优化）

### 🟢 L-1：Node.js 22 为最新版本，长期支持性未知

**严重级别：低**
**位置：** `server/Dockerfile` 第 6 行、`web/Dockerfile` 第 6 行

**问题描述：**
- 使用 `node:22-alpine`，Node.js 22 是 Current 版本（非 LTS）
- 生产环境建议使用 LTS 版本（当前为 Node.js 20，2026-04 后为 Node.js 22 LTS）
- Current 版本更新频繁，可能引入不兼容变化

**建议：** 待 Node.js 22 进入 LTS 后再使用，或使用 Node.js 20 LTS

---

### 🟢 L-2：Prisma 6.x 为最新大版本，需关注稳定性

**严重级别：低**
**位置：** `server/package.json` 第 25、54 行

```json
"@prisma/client": "^6.0.0",
"prisma": "^6.0.0",
```

**问题描述：**
- Prisma 6 是较新的主版本，可能存在未发现的 bug
- `^6.0.0` 允许升级到 6.x 任意小版本，可能引入意外变更

**建议：** 锁定到更精确的版本（如 `~6.0.0`），并关注 Prisma 官方 changelog

---

### 🟢 L-3：缺少数据库连接池配置

**严重级别：低**
**位置：** `server/prisma/schema.prisma`（datasource 中无 connection_limit）

**问题描述：**
- 未配置 Prisma 连接池大小，使用默认值
- 高并发下可能出现连接耗尽
- 缺少 `pool_timeout`、`statement_cache_size` 等调优参数

**建议：**
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  relationMode = "prisma"
}
// 或在 DATABASE_URL 中添加 ?connection_limit=20&pool_timeout=10
```

---

### 🟢 L-4：索引设计尚可，但部分高频查询缺少复合索引

**严重级别：低**
**位置：** `server/prisma/schema.prisma`

**问题说明：**
- `Work` 表常用查询可能是 `type + createdAt` 排序（社区作品列表按时间），目前只有单列索引
- `GenerationLog` 表 `userId + createdAt` 的组合查询很常见（用户生成历史），只有单列索引
- `ModerationLog` 表 `result + createdAt` 组合查询（后台审核列表）缺少复合索引

**建议：** 评估实际查询模式，添加必要的复合索引

---

### 🟢 L-5：缺少速率限制的全局限流配置

**严重级别：低**
**位置：** `server/src/middleware/rate-limit.ts`

**问题说明：**
- 限流按接口组（auth/llm/image/audio/video）分别配置
- 缺少全局的 IP 级别的 API 调用总量限制
- 攻击者可以通过同时调用多个不同接口绕过单组限流

**建议：** 添加一层全局限流（如每 IP 每分钟总请求数上限）

---

### 🟢 L-6：上传目录权限未在 Dockerfile 中设置

**严重级别：低**
**位置：** `server/Dockerfile`

**问题说明：**
- VOLUME 声明了 `/app/uploads`，但未确保目录存在且权限正确
- 如果挂载空目录，可能因权限问题导致上传失败

**建议：** 在 Dockerfile 中 `RUN mkdir -p /app/uploads && chmod 755 /app/uploads`

---

### 🟢 L-7：前端 Dockerfile 缺少非 root 用户

**严重级别：低**
**位置：** `web/Dockerfile`

**问题说明：**
- 前端 Nginx 容器也以 root 运行
- Nginx 官方镜像默认有 `nginx` 用户，可以直接使用

**建议：** 添加 `USER nginx` 或确保 Nginx worker 进程以非 root 运行

---

### 🟢 L-8：前端依赖版本非常新，存在兼容性风险

**严重级别：低**
**位置：** `web/package.json`

```json
"react": "^19.2.8",
"react-router-dom": "^7.18.2",
"vite": "^8.2.2",
"typescript": "~6.0.2",
```

**问题说明：**
- React 19、React Router 7、Vite 8、TypeScript 6 都是非常新的版本
- 虽然功能先进，但生态兼容性可能存在问题
- 部分第三方库可能尚未适配

**建议：** 关注各库的兼容性 issues，确保测试覆盖充分

---

## 五、各维度详细审查

### 5.1 Docker 配置审查

**亮点：**
- 采用多阶段构建，builder 和 runtime 分离
- 使用 alpine 基础镜像，镜像体积较小
- 后端使用 `tini` 作为 init 进程，确保信号正确传递
- 配置了 HEALTHCHECK
- 使用 `npm ci` 保证依赖版本一致
- 后端有 `npm prune --production` 精简生产依赖

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 中 | 容器以 root 用户运行 | `server/Dockerfile`、`web/Dockerfile` |
| 低 | Node.js 22 非 LTS | `server/Dockerfile`、`web/Dockerfile` |
| 低 | 上传目录未预创建 | `server/Dockerfile` |
| 低 | better-sqlite3 构建依赖（python3/make/g++）在 builder 阶段安装，runtime 不包含 | 已正确处理，无问题 |

---

### 5.2 docker-compose 审查

**亮点：**
- 使用 profile 区分最小化/生产/数据库-only 模式
- 所有服务都配置了 healthcheck
- 使用命名卷管理数据持久化
- 网络隔离（独立 bridge 网络）
- 前端 Nginx 反向代理配置完善

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 高 | Redis 无密码且端口暴露 | `docker-compose.yml:106-123` |
| 高 | PostgreSQL 默认密码弱 | `docker-compose.yml:91` |
| 高 | JWT_SECRET 默认值弱 | `docker-compose.yml:28` |
| 中 | 无资源限制（mem/cpu） | 所有服务 |
| 中 | 环境变量直接写在 compose 中，密钥管理分散 | `backend.environment` |
| 低 | backend 和 postgres 之间缺少 depends_on（生产模式） | `docker-compose.yml` |
| 低 | 缺少 restart 策略的具体配置（仅 unless-stopped） | 各服务 |

---

### 5.3 部署流程审查

**亮点：**
- 提供 Docker 和 PM2 两种部署方式
- 部署脚本结构清晰，有颜色输出和步骤提示
- `backup.sh` 支持多种部署方式的自动检测
- 有自动备份的 cron 配置
- 包含 SSL 证书自动申请（certbot）
- 包含防火墙配置（ufw）

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 高 | 默认密码硬编码 | `deploy/server-deploy.sh:330-331` |
| 中 | 无回滚机制 | `deploy/update.sh` |
| 中 | 无 CI/CD | 项目根目录 |
| 中 | 备份仅本地 | `deploy/backup.sh` |
| 低 | deploy.sh 中数据库迁移在 PM2 启动之后（可能短暂不一致） | `deploy/deploy.sh:92` |
| 低 | 缺少部署版本记录 | 所有部署脚本 |

---

### 5.4 环境管理审查

**亮点：**
- 提供了 `.env.example` 模板
- 根目录 `.gitignore` 有明确的密钥安全注释
- 配置项分类清晰（域名/密钥/数据库/AI 供应商/支付等）

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 高 | 真实 API Key 泄露 | `server/.env:32` |
| 高 | `.env.production` 暴露在仓库中 | `.env.production` |
| 中 | 密钥管理分散（compose + .env + deploy/.env.server 多处） | 多处 |
| 中 | 缺少配置验证（启动时检查所有必需环境变量） | 后端入口 |
| 低 | `deploy/.env.server` 与根目录 `.env.production` 内容重复，维护成本高 | 两处配置 |

---

### 5.5 数据库设计审查

**亮点：**
- 使用 Prisma ORM，自动防 SQL 注入
- 外键 + 级联删除定义清晰
- 常用查询字段都有索引
- 有审核日志表（SiteConfigAuditLog）
- 有迁移文件和迁移指南
- 支持 SQLite → PostgreSQL 迁移

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 高 | 默认 SQLite，生产能力有限 | `schema.prisma:8` |
| 中 | 部分复合索引缺失 | `schema.prisma`（Work/GenerationLog/ModerationLog） |
| 中 | 无数据库连接池配置 | `schema.prisma` |
| 中 | 备份方案不完善（SQLite 热备份一致性问题） | `backup.sh` |
| 低 | 密码字段未标注加密方式 | `User.password` |
| 低 | 缺少过期数据清理策略（如旧的 GenerationLog） | 各表 |
| 低 | `remainingTokens` 冗余存储（可由 total - used 计算） | `UserQuota` |

---

### 5.6 监控与日志审查

**亮点：**
- 自研结构化日志工具，支持 JSON/文本双模式
- 日志自动脱敏（密码、Token、邮箱、手机号等）
- 请求 ID 链路追踪（虽然实现有 bug）
- 慢请求告警（>1s warn, >3s error）
- 健康检查分 liveness/readiness 两级
- 优雅停机机制完善

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 中 | traceId 使用 globalThis，并发下错乱 | `logger.ts`、`request-id.ts` |
| 中 | 错误处理中间件用 console.error | `error.ts:10` |
| 低 | 无集中化日志收集（ELK/Loki 等） | - |
| 低 | 无应用性能监控（APM） | - |
| 低 | 无业务指标监控（用户数、生成量、配额消耗等） | - |

---

### 5.7 安全基线审查

**亮点：**
- CORS 白名单配置，生产环境强制 HTTPS
- Helmet 安全头配置完善
- 速率限制支持 Redis 分布式 + 动态调整
- JWT 认证 + RBAC 角色体系
- 唯一超级管理员不变量约束
- Zod 参数校验（部分路由接入）
- 文件上传白名单 + 随机文件名
- 内容审核双审核体系
- HSTS 生产环境启用

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 高 | CORS 子域名匹配可绕过 | `index.ts:67-76` |
| 高 | 文件上传无内容验证 | `upload.ts:21-28` |
| 中 | bcryptjs 替代原生 bcrypt | `package.json:27` |
| 中 | 全局限流缺失 | `rate-limit.ts` |
| 中 | Zod 校验未覆盖全部路由 | 多路由文件 |
| 低 | 缺少登录失败次数限制和账号锁定 | auth 路由 |
| 低 | 缺少请求体大小的分路由限制（全局 10MB 统一） | `index.ts:136` |

---

### 5.8 依赖安全审查

**后端依赖（server/package.json）：**

| 依赖 | 版本 | 风险评估 |
|------|------|----------|
| express | ^4.21.0 | 稳定，4.x 长期维护 |
| @prisma/client | ^6.0.0 | 较新大版本，注意稳定性 |
| prisma | ^6.0.0 | 同上 |
| jsonwebtoken | ^9.0.2 | 稳定，但建议迁移到 jose |
| bcryptjs | ^2.4.3 | ⚠️ 长期未维护（2017年），建议替换 |
| helmet | ^8.3.0 | 最新版，良好 |
| express-rate-limit | ^8.6.2 | 最新版，良好 |
| ioredis | ^6.0.0 | 最新版，良好 |
| zod | ^4.5.4 | 较新大版本 |
| multer | ^1.4.5-lts.1 | LTS 版本，良好 |
| nodemailer | ^9.1.1 | 最新版，良好 |

**前端依赖（web/package.json）：**

| 依赖 | 版本 | 风险评估 |
|------|------|----------|
| react | ^19.2.8 | ⚠️ 非常新，生态兼容性需关注 |
| react-dom | ^19.2.8 | 同上 |
| react-router-dom | ^7.18.2 | ⚠️ 非常新 |
| vite | ^8.2.2 | ⚠️ 非常新 |
| typescript | ~6.0.2 | ⚠️ 非常新 |
| zustand | ^5.0.15 | 最新版，良好 |
| konva | ^10.3.1 | 稳定 |
| react-konva | ^19.2.5 | 跟随 React 19 更新 |

**问题汇总：**

| 级别 | 问题 | 位置 |
|------|------|------|
| 高 | bcryptjs 长期未维护（2017 年至今无更新） | `server/package.json:27` |
| 中 | 前端依赖版本过新，兼容性风险 | `web/package.json`（多处） |
| 中 | 缺少依赖漏洞扫描流程（npm audit / Snyk） | CI/CD 缺失 |
| 低 | jsonwebtoken 可迁移到 jose（更现代的 JWT 库） | `server/package.json:35` |
| 低 | 无 lockfile 完整性检查（npm ci --lockfile-version） | 构建流程 |

---

## 六、按优先级排序的优化建议

### P0 - 立即修复（生产环境上线前必须完成）

1. **[H-1] 吊销泄露的智谱 API Key**
   - 立即在智谱控制台吊销 `761491ceae...` 这个 Key
   - 检查 Git 历史中是否有其他密钥泄露
   - 将 `server/.env` 加入 `.gitignore` 并确认已忽略

2. **[H-3] 修复默认密码问题**
   - 部署脚本首次部署时生成随机密码
   - 种子数据密码从环境变量读取
   - 首次登录强制修改密码

3. **[H-4] Redis 安全加固**
   - 添加 Redis 密码认证
   - 移除公网端口映射（或绑定 127.0.0.1）
   - 启用 protected-mode

4. **[H-5] 修复 CORS 子域名绕过**
   - 将 `endsWith` 改为 `===` 或 `.endsWith('.' + domain)` 精确匹配

5. **[H-2] 清理敏感配置文件**
   - 重命名 `.env.production` 为 `.env.production.example`
   - 确认所有 `.env*`（除 `.env.example`）均被 gitignore

### P1 - 一周内修复

6. **[H-6] 文件上传安全加固**
   - 添加文件魔数检测（使用 `file-type` 库）
   - 图片文件使用 sharp 二次编码处理
   - SVG 文件进行 XML 净化

7. **[M-1] Docker 容器非 root 化**
   - 前后端 Dockerfile 均添加非 root 用户
   - 确保文件权限正确设置

8. **[M-2] 添加 docker-compose 资源限制**
   - 为所有服务配置 CPU/内存限制
   - 特别关注后端和 PostgreSQL 的内存配置

9. **[M-6] 替换 bcryptjs 为 bcrypt 或 argon2**
   - 使用原生 bcrypt 提升安全性和性能
   - 或使用 argon2（OWASP 推荐）

10. **[M-7] 修复 traceId 并发安全问题**
    - 使用 `AsyncLocalStorage` 替代 `globalThis`
    - 确保日志链路追踪正确

### P2 - 一个月内修复

11. **[M-3] 建立部署回滚机制**
    - Docker 模式：镜像版本化，保留最近 N 个版本
    - PM2 模式：部署前备份 dist，提供 rollback 脚本
    - 考虑蓝绿部署策略

12. **[M-4] 搭建 CI/CD 流程**
    - 添加 GitHub Actions 配置
    - 至少包含 typecheck + lint + build
    - 生产部署手动触发

13. **[M-5] 备份策略升级**
    - 启用异地备份（S3/OSS）
    - SQLite 热备份改用 `.backup` 命令
    - 定期备份恢复演练

14. **[M-8] 数据库切换到 PostgreSQL**
    - 将 schema 默认 provider 改为 PostgreSQL
    - 完善 PostgreSQL 迁移文档
    - 生产环境强制使用 PostgreSQL

15. **[M-9] 统一错误日志输出**
    - error 中间件使用 logger 替代 console.error
    - 确保所有错误都有 traceId 关联

### P3 - 长期优化

16. **[L-1] Node.js 版本降级到 LTS**
    - 等待 Node.js 22 进入 LTS 或使用 Node.js 20

17. **[L-4] 添加复合索引优化查询**
    - 根据实际查询模式评估并添加复合索引

18. **[L-5] 全局限流配置**
    - 添加 IP 级别的总请求数限制

19. **[L-10] 集中化日志和监控**
    - 接入 ELK/Loki 进行日志集中管理
    - 接入 Prometheus + Grafana 进行指标监控
    - 配置告警规则（错误率、响应时间、资源使用）

20. **[L-11] 安全审计自动化**
    - 集成 npm audit / Snyk 依赖扫描
    - 定期安全扫描（OWASP ZAP 等）
    - 安全头自动验证

---

## 七、总结

项目整体的安全意识和工程质量处于**中等偏上**水平，尤其在安全中间件（Helmet、CORS、速率限制、JWT、RBAC）、结构化日志、健康检查、优雅停机等方面做得比较完善。

**核心短板：**
1. **敏感信息管理** - 真实 API Key 泄露是最严重的问题
2. **资源隔离与限制** - Docker 容器 root 运行、无资源限制、Redis 无密码
3. **部署可靠性** - 缺少回滚机制、CI/CD、异地备份

建议按照 P0 → P1 → P2 → P3 的优先级逐步修复，P0 级问题在生产环境上线前必须全部解决。
